import React from "react";
import ReactDOM from "react-dom/client";
import App from "./Components/App";
//Render all of the notes inside app.js
ReactDOM.createRoot(document.getElementById('root')).render(<App />);